# Figures (Mermaid Source)

This folder contains mermaid sources for figures referenced in the thesis chapters. These are intended for:
- in-document rendering (Markdown with Mermaid support)
- copy/paste into slide decks that support Mermaid
- later export to SVG/PNG via a Mermaid renderer

Figure numbering follows Chapter 3/4 convention.

