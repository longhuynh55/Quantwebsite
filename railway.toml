[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health/data?probe=true&canary=true&includeFundamentals=false"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
